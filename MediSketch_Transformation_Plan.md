# MediSketch Transformation Plan (Project: BananaFace -> MediSketch)

## 1. Vision & Goal
**Objective**: Transform the existing "BananaFace" (AI Stylist) into **"MediSketch"** (AI Medical Illustrator).

**Core Value**: Provide copyright-free, customizable, professional-grade medical illustrations for education, research, and clinical communication.
**Target Audience**: Medical students, Bio-researchers, Clinical doctors.

## 2. Architecture Analysis
*   **Current Flow**: `User Options (Gender, Age...)` -> `Gemini API` -> `Portrait Image`
*   **Target Flow**: `Medical Parameters (System, View...)` -> `Gemini API (with Medical System Prompt)` -> `Medical Illustration`

## 3. Implementation Steps

### Phase 1: Data Layer Transformation (`constants.ts`)
Replace the existing `CONTROL_GROUPS` (Gender, Age, Nationality, etc.) with Medical Control Groups.

*   **Group 1: Anatomical System (解剖系统)**
    *   *Options*: Cardiovascular (心血管), Skeletal (骨骼), Nervous (神经), Muscular (肌肉), Digestive (消化), Respiratory (呼吸), Cellular (细胞微观).
*   **Group 2: View/Perspective (视角/切面)**
    *   *Options*: Anterior (前视), Posterior (后视), Lateral (侧视), Cross-section (横切面), Longitudinal section (纵切面), Microscopic (显微镜下).
*   **Group 3: Pathology/Condition (病理状态)**
    *   *Options*: Healthy (健康), Inflamed (发炎), Fractured (骨折), Tumorous (肿瘤), Necrotic (坏死), Post-operative (术后).
*   **Group 4: Art Style (艺术风格)**
    *   *Options*: Realistic Textbook (写实教科书), Line Art (黑白线稿), 3D Render (3D 渲染), Schematic (扁平示意图), MRI/CT Style (影像风格).

### Phase 2: Logic Layer & Prompt Engineering (`App.tsx`)
Modify the `buildPrompt` function to inject a strong system prompt to ensure style consistency.

*   **System Prompt Injection**:
    ```text
    "Professional medical illustration, scientifically accurate, clean white background, high resolution, educational purpose, detailed anatomy."
    ```
*   **Logic Update**:
    Combine `System Prompt` + `User Selected Options` + `User Text Input`.

### Phase 3: UI & Branding (`Header.tsx`, `index.html`, `package.json`)
*   **Title**: Change "BananaFace" to "MediSketch".
*   **Subtitle**: Change "Your Pocket Stylist" to "AI Medical Illustrator".
*   **Meta Info**: Update `index.html` title and `package.json` name.

## 4. File Modification List
1.  **`constants.ts`**: Complete rewrite of control groups.
2.  **`App.tsx`**: Update prompt generation logic to include medical context.
3.  **`components/Header.tsx`**: Update title and text.
4.  **`index.html`**: Update page title.
5.  **`package.json`**: Update project name.
6.  **`README.md`**: Update documentation to reflect the new tool.

## 5. Execution Strategy
1.  **Apply Constants**: Modify `constants.ts` first. This will immediately change the dropdowns in the UI.
2.  **Apply Logic**: Tune the prompt in `App.tsx` to ensure the AI behaves like a medical illustrator.
3.  **Rebrand**: Finalize UI text changes to complete the transformation.

---
*Plan generated by GitHub Copilot (Ruthless PM Mode)*
